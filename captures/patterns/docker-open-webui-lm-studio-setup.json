{
  "id": "captures/patterns/docker-open-webui-lm-studio-setup",
  "path": "captures/patterns/docker-open-webui-lm-studio-setup.md",
  "title": "Connecting Open-WebUI to LM Studio via Docker",
  "category": "captures/patterns",
  "type": "patterns",
  "summary": "Troubleshooting guide for connecting Open-WebUI (Docker) to LM Studio on a remote machine. Three issues were identified: port mismatch between socat listener and configured URL, incorrect API type (Ollama vs OpenAI-compatible), and socat not persisting across reboots. The fix uses OPENAI_API_BASE_URL environment variable, a systemd service for socat persistence, and correct port alignment. Covers docker-compose configuration, socat proxy setup, and verification steps.",
  "keywords": ["open-webui", "lm studio", "docker", "socat", "openai api", "docker-compose", "systemd", "port forwarding"],
  "entities": ["Open-WebUI", "LM Studio", "CMK3888", "Orchemy 3.8", "Ollama", "Debian", "OMV7", "socat"],
  "see_also": [],
  "source": "inbox/chats/docker-open-webui-setup.md",
  "processed_at": "2026-07-09",
  "chunks": [
    {
      "section": "Problem Setup",
      "content": "## Problem Setup\n\nThe user runs Open-WebUI in Docker on a CMK3888 (ARM, Debian/OMV7) and wants to reach LM Studio on a remote Orchemy 3.8 machine at `http://nyx.local:1234/v1/models`."
    },
    {
      "section": "Issues Identified",
      "content": "## Issues Identified\n\n1. **Port Mismatch**: socat was listening on port 1234 but Open-WebUI was configured to use port 1235. The URL inside Open-WebUI must be `http://nyx:1234/v1`.\n2. **host-gateway mapping**: `extra_hosts: nyx:host-gateway` maps `nyx` to the Docker host's IP (the CMK3888), not the remote machine. This is architecturally correct — the path is container → CMK3888 host → socat → nyx.local.\n3. **LM Studio is NOT Ollama**: Open-WebUI has two separate connection types — Ollama uses the Ollama native API at `http://host:11434`, while LM Studio uses the OpenAI-compatible API at `http://host:1234/v1`. Must configure as OpenAI API connection."
    },
    {
      "section": "Correct docker-compose.yml",
      "content": "## Correct docker-compose.yml\n\n```yaml\nservices:\n  open-webui:\n    image: ghcr.io/open-webui/open-webui:main\n    container_name: open-webui\n    volumes:\n      - /srv/dev-disk-by-uuid-91eed694-1b39-4351-98e9-0eb4f8066865/containers/config/openwebui:/app/backend/data\n      - /var/run/dbus:/var/run/dbus\n      - /var/run/avahi-daemon/socket:/var/run/avahi-daemon/socket\n    extra_hosts:\n      - \"nyx:host-gateway\"\n    environment:\n      WEBUI_SECRET_KEY: ${WEBUI_SECRET_KEY}\n      # LM Studio — OpenAI-compatible, NOT Ollama\n      OPENAI_API_BASE_URL: \"http://nyx:1234/v1\"\n      OPENAI_API_KEY: \"lm-studio\"\n      # LM Studio ignores this, but it's required\n      # Disable Ollama since you're not using it\n      ENABLE_OLLAMA_API: \"false\"\n    restart: unless-stopped\n    ports:\n      - \"3000:8080\"\n```"
    },
    {
      "section": "Persistent socat",
      "content": "## Persistent socat\n\nRunning `socat ... &` will die on reboot. Use a systemd unit on the CMK3888:\n\n```ini\n# /etc/systemd/system/socat-lmstudio.service\n[Unit]\nDescription=Socat proxy to nyx LM Studio\nAfter=network.target\n\n[Service]\nExecStart=/usr/bin/socat TCP-LISTEN:1234,fork,reuseaddr TCP:nyx.local:1234\nRestart=always\nRestartSec=5\n\n[Install]\nWantedBy=multi-user.target\n```\n\n```bash\nsudo systemctl daemon-reload\nsudo systemctl enable --now socat-lmstudio\n```"
    },
    {
      "section": "Verification",
      "content": "## Verification\n\nFrom the CMK3888 host (outside Docker):\n\n```bash\ncurl http://localhost:1234/v1/models\n```\n\nFrom inside the running container:\n\n```bash\ndocker exec -it open-webui curl http://nyx:1234/v1/models\n```\n\nBoth should return the model list JSON. If the second fails, the host-gateway resolution isn't working — replace `host-gateway` with the CMK3888's actual LAN IP:\n\n```yaml\nextra_hosts:\n  - \"nyx:192.168.x.x\"  # your CMK3888's actual IP\n```"
    },
    {
      "section": "Summary of Changes",
      "content": "## Summary of Changes\n\n| Problem | Fix |\n|---|---|\n| Port typo 1235 → 1234 | Fixed in env var |\n| Wrong API type (Ollama vs OpenAI) | Use `OPENAI_API_BASE_URL` instead |\n| socat dies on reboot | systemd service |\n| Ollama errors in UI | Set `ENABLE_OLLAMA_API=false` |"
    }
  ]
}
